/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "i.pinimg.com",
      "arrivls.com",
      "example.com",
      "a0.muscache.com",
      "images.unsplash.com",
    ], // Add only the hostname
  },
};

export default nextConfig;
